{
  "permissions": {
    "deny": ["Read(.env.*)", "Read(**/*token*)", "Read(**/*key*)"]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "pnpm format:fix && pnpm lint:fix"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'echo \"Running final lint check...\"; if ! pnpm lint:fix 2>/dev/null; then echo \"Lint errors detected. Continuing to fix...\"; pnpm lint 2>&1 | head -10; exit 2; else echo \"All lint checks passed!\"; fi'"
          }
        ]
      }
    ]
  }
}
