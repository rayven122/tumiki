name: 'Deploy to Vercel'
description: 'Deploy to Vercel (staging or production)'

inputs:
  stage:
    description: 'staging or production'
    required: true
  vercel_token:
    description: 'Vercel token'
    required: true

outputs:
  url:
    description: 'Deployment URL'
    value: ${{ steps.deploy.outputs.url }}

runs:
  using: 'composite'
  steps:
    - name: Deploy
      id: deploy
      shell: bash
      env:
        VERCEL_TOKEN: ${{ inputs.vercel_token }}
      run: |
        # Use pnpm exec vercel (Vercel CLI is installed as devDependency)
        echo "🚀 デプロイを開始します（ステージ: ${{ inputs.stage }}）..."
        if [ "${{ inputs.stage }}" = "production" ]; then
          echo "📍 本番環境へデプロイ"
          OUTPUT=$(pnpm exec vercel deploy --prod --yes --token="$VERCEL_TOKEN" 2>&1) || DEPLOY_EXIT_CODE=$?
        elif [ "${{ inputs.stage }}" = "staging" ]; then
          echo "📍 ステージング環境へデプロイ"
          OUTPUT=$(pnpm exec vercel deploy --target staging --yes --token="$VERCEL_TOKEN" 2>&1) || DEPLOY_EXIT_CODE=$?
        else
          echo "📍 プレビュー環境へデプロイ"
          OUTPUT=$(pnpm exec vercel deploy --yes --token="$VERCEL_TOKEN" 2>&1) || DEPLOY_EXIT_CODE=$?
        fi

        # Show deployment output
        echo "📋 デプロイ出力:"
        echo "$OUTPUT"

        # Check if deployment failed
        if [ -n "$DEPLOY_EXIT_CODE" ] && [ "$DEPLOY_EXIT_CODE" -ne 0 ]; then
          echo "❌ Vercelデプロイが失敗しました（終了コード: $DEPLOY_EXIT_CODE）"
          exit 1
        fi

        # Extract URL (try multiple patterns)
        VERCEL_URL=$(echo "$OUTPUT" | grep -oE 'https://[a-zA-Z0-9.-]+\.vercel\.app' | head -1)

        if [ -z "$VERCEL_URL" ]; then
          echo "❌ デプロイURLが出力から見つかりませんでした"
          echo "完全な出力:"
          echo "$OUTPUT"
          exit 1
        fi

        # Set URL based on environment
        if [ "${{ inputs.stage }}" = "production" ]; then
          URL="https://www.tumiki.cloud"
          echo "✅ デプロイ完了"
          echo "🌐 本番URL: $URL"
          echo "📦 Vercel URL: $VERCEL_URL"
        elif [ "${{ inputs.stage }}" = "staging" ]; then
          URL="https://stg.tumiki.cloud"
          echo "✅ デプロイ完了"
          echo "🌐 ステージングURL: $URL"
          echo "📦 Vercel URL: $VERCEL_URL"
        else
          # Preview環境ではVercel URLをそのまま使用
          URL="$VERCEL_URL"
          echo "✅ デプロイ完了: $URL"
        fi

        echo "url=$URL" >> $GITHUB_OUTPUT
