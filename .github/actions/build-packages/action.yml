name: Build Packages
description: デプロイに必要なパッケージ（vitest-config、db、auth、utils、mailer）をビルドします

runs:
  using: composite
  steps:
    - name: 🔧 Build tooling packages
      shell: bash
      run: |
        echo "Building tooling packages..."
        pnpm run --filter @tumiki/vitest-config build

    - name: 📦 Build core packages
      shell: bash
      run: |
        echo "Building core packages..."
        pnpm run --filter @tumiki/db build
        pnpm run --filter @tumiki/auth build
        pnpm run --filter @tumiki/utils build
        pnpm run --filter @tumiki/mailer build

    - name: ✅ Verify builds
      shell: bash
      run: |
        echo "Verifying package builds..."
        for pkg in vitest-config db auth utils mailer; do
          if [ ! -d "packages/$pkg/dist" ] && [ ! -d "tooling/$pkg/dist" ]; then
            echo "Warning: Build output not found for @tumiki/$pkg"
          else
            echo "✓ @tumiki/$pkg built successfully"
          fi
        done
