name: CI

on:
  pull_request:
    branches:
      - main
  push:
    branches: [main]

jobs:
  ci:
    name: 🚀 CI
    runs-on:
      group: self-arc

    env:
      NODE_OPTIONS: --max-old-space-size=4096
      PRISMA_FIELD_ENCRYPTION_KEY: k1.aesgcm256.kG588VHXtTCncg2y9KmbXqyssLulxqUjrSn9_TXFu2U=
    steps:
      - name: 📥 Checkout branch
        uses: actions/checkout@v4

      - name: ⚙️ Setup
        uses: ./.github/actions/setup

      - name: 💾 Cache Turbo
        uses: actions/cache@v4
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-turbo-

      - name: 🔧 Build packages
        run: pnpm turbo build
        # Turboを使用して依存関係を考慮した並列ビルド

      - name: ✅ Run lint, format and typecheck
        run: pnpm check

      - name: 🧪 Run tests
        run: pnpm test
        # packages/dbのテストは除外（PostgreSQLコンテナが必要なため）
