# 自然言語によるAIエージェント設定機能 設計書

## 概要

GPTsのように、自然言語での会話を通じてAIエージェントを構築できる機能を実装する。
ユーザーが「〇〇ができるエージェントを作りたい」と伝えると、AIがヒアリングしながらMCPツールセットの選択とシステムプロンプトの生成を支援する。

## アーキテクチャ

```
┌─────────────────────────────────────────────────────────────────┐
│                    Frontend (manager)                           │
│  apps/manager/src/app/[orgSlug]/agents/create-with-ai/         │
│  ├── page.tsx                                                  │
│  └── _components/                                              │
│      ├── AgentBuilderPageClient.tsx (メイン)                   │
│      ├── AgentBuilderChat.tsx (会話UI)                        │
│      ├── AgentConfigPreview.tsx (リアルタイムプレビュー)       │
│      └── McpRecommendationCard.tsx (MCP推薦カード)            │
│                              │                                  │
│  atoms/agentBuilderAtoms.ts (Jotai状態管理)                    │
└──────────────────────────────│──────────────────────────────────┘
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Backend (mcp-proxy)                          │
│  apps/mcp-proxy/src/features/agentBuilder/                     │
│  ├── route.ts (POST /agent-builder/chat)                       │
│  ├── schema.ts (リクエスト/レスポンススキーマ)                  │
│  ├── prompts.ts (各ステップ用システムプロンプト)               │
│  └── mcpRecommender.ts (MCP推薦ロジック)                       │
└─────────────────────────────────────────────────────────────────┘
```

## 会話フロー（4ステップ）

```
[Step 1: ヒアリング] → [Step 2: ツール選択] → [Step 3: プロンプト生成] → [Step 4: 確認・作成]
```

### Step 1: ヒアリング

- ユーザーの要望を自然言語でヒアリング
- 目的・用途、対象タスク、制約条件を明確化
- 最大2往復の質問で要件を整理

### Step 2: MCPツール選択

- Dynamic Search的アプローチでMCPサーバーを推薦
- 関連度スコアと推薦理由を表示
- ユーザーが追加/削除を指示可能

### Step 3: システムプロンプト生成

- 要件とMCPツールに基づいてプロンプトを自動生成
- エージェント名・説明も提案
- ユーザーが微調整可能

### Step 4: 確認・作成

- 設定の最終確認
- 既存の`api.v2.agent.create`で作成実行

## データ構造

### 会話状態（Jotai atom）

```typescript
// apps/manager/src/atoms/agentBuilderAtoms.ts

type AgentBuilderStep =
  | "hearing"
  | "tool_selection"
  | "prompt_generation"
  | "confirmation";

type HearingResult = {
  purpose: string;
  targetTasks: string[];
  constraints?: string;
  additionalNotes?: string;
};

type McpRecommendation = {
  mcpServerId: string;
  mcpServerName: string;
  relevanceScore: number;
  reason: string;
  isSelected: boolean;
};

type GeneratedAgentConfig = {
  name: string;
  description: string;
  systemPrompt: string;
  modelId: string;
  visibility: "PRIVATE" | "ORGANIZATION" | "PUBLIC";
  mcpServerIds: string[];
};

type AgentBuilderState = {
  currentStep: AgentBuilderStep;
  conversationId: string;
  hearingResult: HearingResult | null;
  mcpRecommendations: McpRecommendation[];
  selectedMcpServerIds: string[];
  generatedConfig: GeneratedAgentConfig | null;
  messages: ChatMessage[];
};
```

## API設計

### POST `/agent-builder/chat`

既存の`/chat`エンドポイントのパターンを参考に、ストリーミング対応のエンドポイントを新設。

**リクエスト:**

```typescript
{
  conversationId: string;
  organizationId: string;
  message: { id: string; role: 'user'; parts: Array<{ type: 'text'; text: string }> };
  currentStep: AgentBuilderStep;
  context: {
    hearingResult?: HearingResult;
    selectedMcpServerIds?: string[];
    availableMcpServers?: McpServerInfo[];
  };
}
```

**レスポンス:** UIMessageStream形式（ストリーミング）

- 通常のテキスト応答
- ステップ完了時に構造化データを含むパート

## UI設計

```
┌────────────────────────────────────────────────────────────────────┐
│ ← 戻る               AIでエージェントを作成                         │
├────────────────────────────────────────────────────────────────────┤
│ ┌──────────────────────────┐ ┌──────────────────────────────────┐ │
│ │     チャットエリア        │ │     プレビューエリア              │ │
│ │                          │ │                                  │ │
│ │ [AI] どのようなエージェント│ │ 📝 エージェント設定               │
│ │     を作りたいですか？    │ │ 名前: (未設定)                   │
│ │                          │ │ 説明: (未設定)                   │
│ │ [User] GitHubのPRを...   │ │                                  │ │
│ │                          │ │ 🔧 選択中のMCP                   │
│ │ ┌────────────────────┐   │ │ (未選択)                         │
│ │ │ GitHub  関連度:95% │   │ │                                  │
│ │ │ [追加] [詳細]      │   │ │ 📋 システムプロンプト             │
│ │ └────────────────────┘   │ │ (未生成)                         │
│ │                          │ │                                  │
│ │ [メッセージ入力] [送信]   │ │ [エージェントを作成] (disabled)   │
│ └──────────────────────────┘ └──────────────────────────────────┘ │
│ ステップ: [1] → [2] → [3] → [4]                                   │
└────────────────────────────────────────────────────────────────────┘
```

## 実装ファイル一覧

### 新規作成

**Backend (mcp-proxy):**

- `apps/mcp-proxy/src/features/agentBuilder/route.ts`
- `apps/mcp-proxy/src/features/agentBuilder/schema.ts`
- `apps/mcp-proxy/src/features/agentBuilder/prompts.ts`
- `apps/mcp-proxy/src/features/agentBuilder/mcpRecommender.ts`
- `apps/mcp-proxy/src/features/agentBuilder/index.ts`

**Frontend (manager):**

- `apps/manager/src/app/[orgSlug]/agents/create-with-ai/page.tsx`
- `apps/manager/src/app/[orgSlug]/agents/create-with-ai/_components/AgentBuilderPageClient.tsx`
- `apps/manager/src/app/[orgSlug]/agents/create-with-ai/_components/AgentBuilderChat.tsx`
- `apps/manager/src/app/[orgSlug]/agents/create-with-ai/_components/AgentConfigPreview.tsx`
- `apps/manager/src/app/[orgSlug]/agents/create-with-ai/_components/McpRecommendationCard.tsx`
- `apps/manager/src/atoms/agentBuilderAtoms.ts`

### 参照・再利用

| ファイル                                                   | 用途                     |
| ---------------------------------------------------------- | ------------------------ |
| `apps/mcp-proxy/src/features/chat/route.ts`                | ストリーミングAPIパターン |
| `apps/mcp-proxy/src/features/chat/chatJwtAuth.ts`          | JWT認証ロジック          |
| `apps/manager/src/atoms/agentFlowAtoms.ts`                 | Jotai状態管理パターン    |
| `apps/manager/src/components/chat.tsx`                     | useChatフック使用パターン |
| `apps/manager/src/server/api/routers/v2/agent/create.ts`   | エージェント作成API      |

## 実装フェーズ

### Phase 1: バックエンドAPI（3-4日）

1. `agentBuilder/` ディレクトリ作成
2. `schema.ts`: 入出力スキーマ定義
3. `prompts.ts`: 各ステップ用プロンプト定義
4. `mcpRecommender.ts`: MCP推薦ロジック（generateObject使用）
5. `route.ts`: ストリーミングエンドポイント実装
6. `app.ts`にルート追加
7. ユニットテスト

### Phase 2: 状態管理（1日）

1. `agentBuilderAtoms.ts`: Jotai atom定義
2. カスタムフック実装（useAgentBuilder）

### Phase 3: フロントエンドUI（3-4日）

1. `page.tsx`: ページエントリ
2. `AgentBuilderPageClient.tsx`: メインコンポーネント
3. `AgentBuilderChat.tsx`: チャットUI（useChat使用）
4. `AgentConfigPreview.tsx`: リアルタイムプレビュー
5. `McpRecommendationCard.tsx`: MCP推薦カード
6. エージェント一覧への導線追加

### Phase 4: 統合・テスト（2日）

1. E2Eテスト作成
2. エッジケース対応
3. UI調整

## 検証方法

1. **ユニットテスト**

   - プロンプト生成ロジック
   - MCP推薦ロジック
   - スキーマバリデーション

2. **手動テスト**

   - 会話フローの動作確認
   - 各ステップの遷移
   - エージェント作成の完了確認

3. **品質チェック**
   ```bash
   pnpm typecheck
   pnpm test
   pnpm build
   ```

## 設計上の決定事項

| 項目           | 決定                 | 理由                                             |
| -------------- | -------------------- | ------------------------------------------------ |
| ライセンス     | CE版でも利用可能     | 全ユーザーが自然言語でエージェント設定可能       |
| 既存UIとの関係 | 並行提供             | 「AIで作成」と「手動で作成」の2つの導線を提供    |
| API配置        | mcp-proxy            | 既存chatRouteパターンの再利用、ストリーミング最適化 |
| 状態管理       | Jotai（フロント）    | 実装がシンプル、DB変更不要                       |
| MCP推薦        | AI検索               | 自然言語での柔軟な検索を実現                     |
| 会話永続化     | なし                 | 通常1セッションで完了するため不要                |

## UI導線の設計

エージェント一覧ページに2つの作成導線を追加：

```
┌─────────────────────────────────────────────────────────────┐
│  エージェント一覧                                            │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────┐  ┌─────────────────────┐          │
│  │ ✨ AIで作成         │  │ ⚙️ 手動で作成        │          │
│  │ 会話しながら設定    │  │ 従来の設定フォーム   │          │
│  └─────────────────────┘  └─────────────────────┘          │
│                                                             │
│  [エージェント一覧...]                                       │
└─────────────────────────────────────────────────────────────┘
```

- 「AIで作成」→ `/agents/create-with-ai`（新規）
- 「手動で作成」→ `/agents/create`（既存）
