# Tumiki MCP プロキシ設定（ローカル環境）
export TUMIKI_PROXY_URL="http://localhost:8080/mcp/YOUR_USER_ID"
export TUMIKI_API_KEY="your_tumiki_api_key_here"

# Context7 直接接続設定
export CONTEXT7_URL="https://mcp.context7.com/mcp"
# 注意: Context7 は API キー不要（認証なし）

# Vegeta 負荷テスト設定（ローカル環境なので高めに設定）
export WARMUP_DURATION="5s"
export WARMUP_RATE="10"
export LOAD_DURATION="30s"
export LOAD_RATE="20"
export STRESS_DURATION="60s"
export STRESS_RATE="50"
export REQUEST_TIMEOUT="30s"

# 結果出力設定
export RESULTS_DIR="./benchmark/results"
