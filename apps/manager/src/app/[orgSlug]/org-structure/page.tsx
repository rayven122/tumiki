import { OrgStructureClient } from "./_components/OrgStructureClient";

/**
 * 組織構造編集ページ
 *
 * Server Component
 * - OrgStructureClientをレンダリング
 */
export default async function OrgStructurePage() {
  return <OrgStructureClient />;
}
